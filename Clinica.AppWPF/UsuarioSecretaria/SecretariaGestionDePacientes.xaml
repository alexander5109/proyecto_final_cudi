<Window
	x:Class="Clinica.AppWPF.UsuarioSecretaria.SecretariaGestionDePacientes"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Title="Clinica Medica – Gestión Integral"
	WindowStartupLocation="CenterScreen"
	Width="1366"
	Height="768"
	Icon="pack://application:,,,/PublishedResources/icons/icono.png"
	WindowStyle="ThreeDBorderWindow">
	<Window.Background>
		<ImageBrush
			ImageSource="pack://application:,,,/PublishedResources/images/otroFondo.png" />
	</Window.Background>
	<!-- ===================== LAYOUT GENERAL ===================== -->
	<Grid
		HorizontalAlignment="Center"
		VerticalAlignment="Center">
		<Grid.RowDefinitions>
			<RowDefinition
				Height="Auto" />
			<RowDefinition
				Height="Auto" />
			<RowDefinition
				Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition
				Width="Auto" />
			<ColumnDefinition
				Width="Auto" />
			<ColumnDefinition
				Width="Auto" />
		</Grid.ColumnDefinitions>
		<Label
			Style="{StaticResource LabelLindo}"
			Grid.Row="0"
			Grid.ColumnSpan="3"
			Content="Gestión de Pacientes"
			FontSize="60"
			FontFamily="Ringbearer"
			FontWeight="Bold"
			HorizontalAlignment="Center"
			Margin="10,35,10,10" />
		<!-- ===================== PANEL 2 - ESTADO DEL TURNO ===================== -->
		<!-- ===================== PANEL 3 - BUSCAR PACIENTE ===================== -->
		<!-- ===================== BOTONES INFERIORES ===================== -->
		<StackPanel
			Grid.Row="2"
			Grid.Column="0"
			Orientation="Vertical"
			Margin="25,25,25,50">
			<Button
				Content="Home"
				Width="170"
				Height="37"
				Margin="10"
				Click="ButtonHome"
				FontSize="20"
				Style="{StaticResource BotonRedondeado}" />
			<Button
				Content="Salir"
				Width="170"
				Height="37"
				Margin="10"
				Click="ButtonSalir"
				FontSize="20"
				Style="{StaticResource BotonRedondeado}"
				HorizontalAlignment="Center" />
		</StackPanel>
		<ContentControl
			Style="{StaticResource ContentControlLindo}"
			Grid.Column="2"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			VerticalContentAlignment="Center"
			HorizontalContentAlignment="Center"
			Grid.Row="1"
			Margin="15,15,15,15">
			<StackPanel
				Orientation="Vertical"
				HorizontalAlignment="Center"
				Margin="5"
				VerticalAlignment="Center">
				<TextBlock
					FontSize="20"
					FontFamily="Ringbearer"
					FontWeight="Bold"
					HorizontalAlignment="Center"
					VerticalAlignment="Center"><Run
						Text="Paciente" /><Run
						Text=" " /><LineBreak /><Run
						Text="Seleccionado" /></TextBlock>
				<!-- Campos -->
				<TextBlock
					Text="Nombre completo:"
					FontWeight="Bold"
					Margin="0 10 0 0" />
				<TextBlock
					Text="{Binding SelectedPaciente.Displayear}" />
				<TextBlock
					Text="Telefono:"
					FontWeight="Bold"
					Margin="0 10 0 0" />
				<TextBlock
					Text="{Binding SelectedPaciente.Telefono}" />
				<TextBlock
					Text="Email:"
					FontWeight="Bold"
					Margin="0 10 0 0" />
				<TextBlock
					Text="{Binding SelectedPaciente.Email}" />
				<TextBlock
					Text="Domicilio:"
					FontWeight="Bold"
					Margin="0 10 0 0" />
				<TextBlock
					Text="{Binding SelectedPaciente.DomicilioDisplayear}" />
				<TextBlock
					Text="Fecha de Ingreso:"
					FontWeight="Bold"
					Margin="0 10 0 0" />
				<TextBlock
					Text="{Binding SelectedPaciente.FechaIngreso}" />

				<StackPanel
					Margin="15,25,15,15">
					<Button
						Content="Modificar datos"
						Width="180"
						Height="30"
						IsEnabled="{Binding PuedeModificarPaciente}"
						Style="{StaticResource BotonRedondeado}"
						Click="ButtonModificarPaciente" />
					<Button
						Content="Sacarle un turno"
						Width="240"
						Height="50"
						Click="ButtonBuscarDisponibilidades"
						Style="{StaticResource BotonRedondeado}" />
				</StackPanel>
			</StackPanel>
		</ContentControl>
		<ContentControl
			Grid.Row="1"
			Grid.Column="1"
			Style="{StaticResource ContentControlLindo}"
			HorizontalAlignment="Left"
			Margin="15,15,15,15">
			<StackPanel
				Margin="10"
				MinWidth="300"
				HorizontalAlignment="Center">
				<Label
					Content="Buscar paciente"
					FontFamily="Ringbearer"
					FontSize="25"
					FontWeight="Bold"
					HorizontalAlignment="Center" />
				<Label
					Content="Filtrar por Nombre, Apellido o DNI:"
					FontFamily="Palatino Linotype"
					FontWeight="Bold"
					Margin="0,10,0,0" />
				<TextBox
					Text="{Binding FiltroPacientesTexto, UpdateSourceTrigger=PropertyChanged}" />
				<ListView
					ItemsSource="{Binding PacientesList}"
					SelectedItem="{Binding SelectedPaciente}"
					Height="160"
					MinWidth="200"
					MaxWidth="400"
					Margin="0,10"
					ScrollViewer.VerticalScrollBarVisibility="Auto">
					<ListView.View>
						<GridView>
							<GridViewColumn
								Header="Nombre"
								Width="auto"
								DisplayMemberBinding="{Binding Nombre}" />
							<GridViewColumn
								Header="Apellido"
								Width="auto"
								DisplayMemberBinding="{Binding Apellido}" />
							<GridViewColumn
								Header="DNI"
								Width="80"
								DisplayMemberBinding="{Binding Dni}" />
						</GridView>
					</ListView.View>
				</ListView>
				<StackPanel
					Margin="10"
					HorizontalAlignment="Center">
					<Button
						Content="Agregar Paciente"
						Width="180"
						Height="30"
						Click="ButtonAgregarPaciente"
						Style="{StaticResource BotonRedondeado}" />
				</StackPanel>
			</StackPanel>
		</ContentControl>
	</Grid>
</Window>
