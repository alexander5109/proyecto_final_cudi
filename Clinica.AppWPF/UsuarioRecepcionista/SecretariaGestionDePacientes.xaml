<Window
	x:Class="Clinica.AppWPF.UsuarioRecepcionista.RecepcionistaGestionDePacientes"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Title="Clinica Medica – Gestión Integral"
	WindowStartupLocation="CenterScreen"
	Width="1024"
	Height="600"
	Icon="pack://application:,,,/PublishedResources/icons/icono.png">
	<Window.Background>
		<ImageBrush
			ImageSource="pack://application:,,,/PublishedResources/images2025sol/fondoClaro2025.jpg" />
	</Window.Background>
	<!-- ===================== LAYOUT GENERAL ===================== -->
	<Grid
		Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition
				Height="Auto" />
			<!-- Título -->
			<RowDefinition
				Height="*" />
			<!-- Contenido -->
			<RowDefinition
				Height="Auto" />
			<!-- Botones inferiores -->
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition
				Width="2*" />
			<!-- Búsqueda + lista -->
			<ColumnDefinition
				Width="*" />
			<!-- Estado del turno -->
		</Grid.ColumnDefinitions>
		<Label
			Grid.Column="0"
			Grid.Row="0"
			Content="Gestion de Pacientes"
			FontSize="55"
			FontFamily="Segoe UI Historic"
			FlowDirection="RightToLeft"
			Style="{StaticResource LabelLindoConPiu}"
			HorizontalContentAlignment="Center"
			VerticalContentAlignment="Center"
			VerticalAlignment="Center"
			Margin="25,0,25,0"
			FontWeight="Bold" />
		<!-- ===================== PANEL 2 - ESTADO DEL TURNO ===================== -->
		<!-- ===================== PANEL 3 - BUSCAR PACIENTE ===================== -->
		<!-- ===================== BOTONES INFERIORES ===================== -->
		<!-- ===== BOTONES INFERIORES (HOME / SALIR) ===== -->
		<StackPanel
			Grid.Row="2"
			Orientation="Horizontal"
			HorizontalAlignment="Left"
			Margin="15,15,15,15"
			Width="380"
			VerticalAlignment="Bottom">
			<Button
				Content="Home"
				Width="170"
				Height="37"
				Margin="10,0,10,10"
				Click="ButtonHome"
				FontSize="20"
				Style="{StaticResource BotonRedondeado}" />
			<Button
				Content="Salir"
				Width="170"
				Height="37"
				Margin="10,0,10,10"
				Click="ClickBoton_Salir"
				FontSize="20"
				Style="{StaticResource BotonRedondeado}" />
		</StackPanel>
		<StackPanel
			Grid.Column="0"
			Grid.Row="1"
			Margin="25,25,25,25">
			<Label
				Content="Buscar Paciente"
				FontSize="25"
				FontFamily="Segoe UI Historic"
				FlowDirection="RightToLeft"
				Style="{StaticResource LabelLindoConSombra}"
				HorizontalContentAlignment="Center"
				VerticalContentAlignment="Center"
				VerticalAlignment="Center"
				FontWeight="Bold"
				HorizontalAlignment="Center" />
			<Label
				Content="Filtrar por Nombre, Apellido o DNI:"
				FontFamily="Palatino Linotype"
				FontWeight="Bold"
				Margin="10,10,10,0"
				HorizontalAlignment="Center" />
			<TextBox
				Text="{Binding FiltroPacientesTexto, UpdateSourceTrigger=PropertyChanged}"
				Margin="10,0,10,0"
				HorizontalAlignment="Center"
				Width="200" />
			<ListView
				ItemsSource="{Binding PacientesList}"
				SelectedItem="{Binding SelectedPaciente}"
				Height="153"
				Margin="10,10,10,10"
				ScrollViewer.VerticalScrollBarVisibility="Auto"
				HorizontalContentAlignment="Center">
				<ListView.View>
					<GridView>
						<GridViewColumn
							Header="Nombre"
							Width="auto"
							DisplayMemberBinding="{Binding Nombre}" />
						<GridViewColumn
							Header="Apellido"
							Width="auto"
							DisplayMemberBinding="{Binding Apellido}" />
						<GridViewColumn
							Header="DNI"
							Width="80"
							DisplayMemberBinding="{Binding Dni}" />
					</GridView>
				</ListView.View>
			</ListView>
			<StackPanel
				Margin="10"
				HorizontalAlignment="Center">
				<Button
					Content="Agregar Paciente"
					Width="180"
					Height="30"
					Click="ButtonAgregarPaciente"
					Style="{StaticResource BotonRedondeado}" />
			</StackPanel>
		</StackPanel>
		<StackPanel
			Orientation="Vertical"
			HorizontalAlignment="Left"
			Grid.Row="1"
			Grid.RowSpan="2"
			Grid.Column="1"
			VerticalAlignment="Top"
			Margin="5,5,5,5">
			<Label
				Content="Paciente seleccionado"
				FontSize="25"
				FontFamily="Segoe UI Historic"
				FlowDirection="RightToLeft"
				Style="{StaticResource LabelLindoConSombra}"
				HorizontalContentAlignment="Center"
				VerticalContentAlignment="Center"
				VerticalAlignment="Center"
				Margin="25,0,25,0"
				FontWeight="Bold" />
			<!-- Campos -->
			<TextBlock
				Text="Nombre completo:"
				FontWeight="Bold"
				Margin="0 10 0 0"
				HorizontalAlignment="Center" />
			<TextBlock
				Text="{Binding SelectedPacienteNombreCompleto}" />
			<TextBlock
				Text="Telefono:"
				FontWeight="Bold"
				Margin="0 10 0 0"
				HorizontalAlignment="Center" />
			<TextBlock
				Text="{Binding SelectedPaciente.Telefono}" />
			<TextBlock
				Text="Email:"
				FontWeight="Bold"
				Margin="0 10 0 0"
				HorizontalAlignment="Center" />
			<TextBlock
				Text="{Binding SelectedPaciente.Email}" />
			<TextBlock
				Text="Domicilio:"
				FontWeight="Bold"
				Margin="0 10 0 0"
				HorizontalAlignment="Center" />
			<TextBlock
				Text="{Binding SelectedPacienteDomicilioCompleto}" />
			<TextBlock
				Text="Fecha de Ingreso:"
				FontWeight="Bold"
				Margin="0 10 0 0"
				HorizontalAlignment="Center" />
			<TextBlock
				Text="{Binding SelectedPaciente.FechaIngreso}" />
			<StackPanel
				Margin="15,25,15,15">
				<Button
					Content="Modificar datos"
					Width="180"
					Height="30"
					IsEnabled="{Binding HayPacienteSeleccionado}"
					Click="ClickBoton_ModificarPaciente"
					Style="{StaticResource BotonRedondeado}"
					Margin="0,0,0,25" />
				<Button
					Content="Sacarle un turno"
					Width="240"
					Height="50"
					IsEnabled="{Binding HayPacienteSeleccionado}"
					Click="ButtonBuscarDisponibilidades"
					Style="{StaticResource BotonRedondeado}" />
			</StackPanel>
		</StackPanel>
	</Grid>
</Window>
